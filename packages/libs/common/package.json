{
    "name": "@hirikarate/common",
    "version": "1.0.0",
    "description": "Common code.",
    "scripts": {
        "clean": "rimraf dist coverage .nyc_output",
        "compile": "yarn clean && tsc --pretty"
    },
    "devDependencies": {
        "@types/node": "~12.7",
        "rimraf": "^3.0.0",
        "typescript": "~3.7"
    },
    "engines": {
        "node": ">=12.0.0"
    },
    "private": true,
    "main": "./dist/app/index.js"
}
